schema_version: 2
nodes:
  - id: worker_bad_retry_1
    type: agent
    build: pip install -e ./agents/retrybad1
    path: dynamic
    outputs: [out]
    retry:
      max_attempts: 3
      initial_delay_seconds: 0.1
      backoff_multiplier: 2.0
      max_delay_seconds: 3.0
      jitter_ratio: 1.1
