schema_version: 2
nodes:
  - id: a
    type: agent
    build: pip install -e ./a
    path: dynamic
    outputs: [out]
    retry:
      max_attempts: 3
      initial_delay_seconds: 0.1
      backoff_multiplier: 2
      max_delay_seconds: 1
      jitter_ratio: 1.5
