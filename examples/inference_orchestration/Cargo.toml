[package]
name = "inference_orchestration"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
mofa-foundation = { path = "../../crates/mofa-foundation" }
tokio = { version = "1", features = ["full"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }
