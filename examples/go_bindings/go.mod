module mofa-go-examples

go 1.21

// MoFA Go SDK Examples
//
// Prerequisites:
// 1. Generate Go bindings: cd .. && ./generate-go.sh
// 2. The generated bindings will be available in the parent directory
//
// Note: The actual module path will depend on the generated bindings structure
