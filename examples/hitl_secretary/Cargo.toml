[package]
name = "hitl_secretary"
version = "0.1.0"
edition = "2021"
description = "秘书Agent模式使用示例 - 基于事件循环的智能助手"

[dependencies]
mofa-sdk = { path = "../../crates/mofa-sdk" }
tokio = { version = "1", features = ["full"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
serde_json = "1.0"
async-trait = "0.1"
