[package]
name = "gateway_socketio_s3"
version.workspace = true
edition.workspace = true

[[bin]]
name = "gateway_socketio_s3"
path = "src/main.rs"

[dependencies]
mofa-kernel        = { path = "../../crates/mofa-kernel" }
mofa-runtime       = { path = "../../crates/mofa-runtime" }
mofa-gateway       = { path = "../../crates/mofa-gateway", features = ["socketio", "s3"] }
mofa-integrations  = { path = "../../crates/mofa-integrations", features = ["socketio", "s3"] }

tokio.workspace             = true
tracing.workspace           = true
tracing-subscriber.workspace = true
anyhow.workspace            = true

[lints]
workspace = true
